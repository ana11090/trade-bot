data/_backup_old/*.csv filter=lfs diff=lfs merge=lfs -text
# All CSVs inside data/sources — including OHLC and bid/ask ticks
# (XAUUSD_ticks_YYYY_MM.csv). LFS handles the size.
data/sources/**/*.csv filter=lfs diff=lfs merge=lfs -text
